# Approved visual references, profile 1.2

Date: 2026-09-23. These are AI-generated design concepts with fictional records,
not screenshots of an implemented OB list or evidence of functional testing.
The textual rules in `docs/OB_BRAND_PROFILE.md` take precedence over incidental
image text, exact pixel sizes and action labels shortened by the image model.

- `desktop-v1.2.png`: RenoApp-based desktop direction reviewed in the OB task.
  Original generated filename: `exec-c014d199-578e-4463-9163-43dc5fc3f2ce.png`.
- `mobile-v1.2.png`: mobile concept explicitly approved before the request to
  update this profile. Original filename:
  `exec-f3639447-31ca-4af4-b011-e76375a20daf.png`.

Sources for the design: the real RenoApp list CSS and synthetic UI screenshots,
the existing BesiktApp product logo, OB profile colors and the agreed list
direction. Tool: built-in image generation. Images are copied without editing.
`provenance.json` records their SHA-256 checksums during the profile build.

No generated logo replaces the original product logo asset. Use the original
from `assets/logo/` in any implementation, not a crop from these mockups.
